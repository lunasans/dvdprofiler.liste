# Dieses Verzeichnis enthält sensible Konfigurationsdateien

Die `config.php` Datei wird während der Installation automatisch erstellt.

**Sicherheit:**
- Dieses Verzeichnis ist durch `.htaccess` geschützt
- Direkter Zugriff auf Dateien ist blockiert
- Nach Installation sollte `config.php` Rechte 600 haben
